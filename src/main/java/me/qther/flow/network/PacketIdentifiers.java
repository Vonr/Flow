package me.qther.flow.network;

import me.qther.flow.init.Flow;
import net.minecraft.util.Identifier;

public class PacketIdentifiers {
    public static final Identifier LOGIN_MOD_PRESENCE_PACKET_ID = Flow.id("presence");

    public static final Identifier DASH_PACKET_ID = Flow.id("dash");
}
